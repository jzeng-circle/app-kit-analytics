# tools/common

Placeholder for shared helpers that aren't specific to a single explorer or
data source. Today this is empty; candidates as more reports land:

- Token-decimal handling and unit conversion
- Address-cluster / counterparty-graph utilities
- Output formatters (table rendering, markdown helpers)
- Generic API caching wrapper
